<%= render 'shared/card',has_return: true, breadcrumbs: {crumbs: [], active_label: "Vehiculos"} do %>
<div class="row">
  <div class="col-lg-12">
    <%= link_to(new_vehicle_path, class: 'btn btn-primary') do%>
     <%= icon('fas','plus', 'Agregar') %>
     <% end %>
  </div>
</div>
<% if @vehicles.empty? %>
  <br>
  <div class="alert alert-info">
    <strong>&iexcl;Atenci&oacute;n!</strong>
      Aun no se registran vehiculos, has clic en el bot&oacute;n <%= fw_icon('plus') %> para agregar el primero.
  </div>
<% else %>
<div class="row">
    <div class="col-lg-12">
      <br>
      <h5>Listado de vehiculos registrados</h5>
      <hr>
      <div class="table-responsive">
        <table class="table table-condensed">
          <thead>
            <tr>
              <th width='10%'></th>
              <th width='20%'>Placa</th>
              <th width='20%'>Nombre</th>
              <th width='20%'>Marca</th>
              <th width='20%'>Año del modelo</th>
              <th width='20%'>Tipo</th>
            </tr>
          </thead>
          <tbody>
            <% @vehicles.each do |vehicle| %>
              <tr>
                <td>
                  <%= link_to(edit_vehicle_path(vehicle), class: 'btn btn-outline-dark btn-sm') do %>
                    <%= icon('fas', 'pencil-alt', 'editar') %>
                  <% end %>
                </td>
                <td><%= vehicle.plate %></td>
                <td><%= vehicle.name %></td>
                <td><%= vehicle.brand %></td>
                <td><%= vehicle.model_year %></td>
                <td><%= vehicle.vehicle_category.vehicle_type %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
<% end %>
<% end %>
