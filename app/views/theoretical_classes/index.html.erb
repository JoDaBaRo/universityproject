<%= render 'shared/card',has_return: true, breadcrumbs: {crumbs: [], active_label: "Clases"} do %>
<div class="row">
  <div class="col-lg-12">
    <%= link_to(new_theoretical_class_path, class: 'btn btn-primary') do%>
     <%= icon('fas','plus', 'Agregar') %>
     <% end %>
  </div>
</div>
<% if @theoretical_classes.empty? %>
  <br>
  <div class="alert alert-info">
    <strong>&iexcl;Atenci&oacute;n!</strong>
      Aun no se registran clases teoricas, has clic en el bot&oacute;n <%= fw_icon('plus') %> para agregar la primera.
  </div>
<% else %>
<div class="row">
    <div class="col-lg-12">
      <br>
      <h5>Listado de clases teoricas registradas</h5>
      <br>
      <div class="table-responsive">
        <table class="table table-condensed">
          <thead>
            <tr>
              <th width='10%'></th>
              <th width='25%'>Descripcion</th>
              <th width='25%'>Tiempo de duracion</th>
            </tr>
          </thead>
          <tbody>
            <% @theoretical_classes.each do |class_type| %>
                <tr>
                  <td>
                    <%= link_to(edit_theoretical_class_path(class_type), class: 'btn btn-outline-dark btn-sm') do %>
                      <%= icon('fas', 'pencil-alt', 'editar') %>
                    <% end %>
                  </td>
                  <td><%= class_type.description %></td>
                  <td><%= class_type.class_length %></td>
                </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
<% end %>
<% end %>
